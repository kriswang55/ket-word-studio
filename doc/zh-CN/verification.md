# 验收记录

[English](../en/verification.md) | **简体中文** | [繁体中文](../zh-HK/verification.md)

验证日期：2026-09-25。交付版本：3.1.0。

## 自动测试

| 范围 | 结果 |
| --- | --- |
| JavaScript 业务逻辑与语言切换 | 18 项通过 |
| Python 服务、静态 HTTP 预览与 Qt 控件与语言切换 | 23 项通过 |
| 合计 | 41 项通过 |

JavaScript 测试包含免登录初始化、示例标记、词库增改停用、重复验证、身份界面约束、抽样、答案归一化、别名、重复提交、续接、题目快照、错词移除、放弃练习、加权统计、JSON / CSV 导出、公式转义、浏览器存储失败、重置与访客数据隔离。

Python 测试包含对应核心规则、SQLite 并发重复提交、损坏数据库保留、本地静态资源访问与目录边界。Qt 测试通过真实控件打开教师词汇编辑器、填写并保存词条，切回学生后搜索、开始练习、提交正确答案和查看记录。

可复现命令：

```bash
npm test
npm run check:docs
python -m pytest -q
```

Python 测试依赖 `requirements-dev.txt`，Qt 控件测试使用临时数据库。网站测试使用隔离的内存存储。

## 运行验证

- Windows Qt 源码：学生概览与教师词库界面已渲染并检查，截图位于 `doc/images/`。
- Windows 桌面 EXE：使用临时数据目录启动，直接显示学生页面，初始化 178 词与 3 条示例记录，截图保存成功，退出码为 0。
- Windows 网站 EXE：实际启动并返回首页、JavaScript 引擎和 178 词 JSON，验证后关闭测试进程。
- 实际浏览器：直接进入学生页；切到教师新增 `robot / 机器人 / 科技` 和别名 `a robot`；切回学生完成该主题练习，`A ROBOT` 判分正确；刷新后保持 179 词与本次 100% 的成绩记录。
- 页面检查：教师导航只含管理概览、词库管理、练习记录；学生导航只含学习功能；显示名称为 KET Word Studio；浏览器控制台未发现错误。
- 网站根路径与仓库子路径均可加载。所有资源均采用相对地址。
- GitHub Pages：部署工作流成功，正式 HTTPS 地址返回 200；实际浏览器验证学生 / 教师切换及各自导航、完成 1 道练习、`SHOUT` 大写答案判为正确，刷新后保留 100% 的本次成绩；控制台未发现错误。
- 在线快捷方式：Windows `.url` 和 Mac `.webloc` 均指向正式 GitHub Pages 地址，文件格式与目标地址已校验。

## 验证边界

已在 Windows 环境进行测试。没有在真实 macOS 或 Safari 设备上验收，也没有声称提供 macOS 原生安装包。网站提供窄屏样式，但尚未进行完整的移动设备兼容矩阵测试。

网站已发布至 [GitHub Pages](https://kriswang55.github.io/ket-word-studio/)。后续重新部署时，应按 `deployment.md` 检查最终 HTTPS 地址。

## 提交内容

源码目录、两种 Windows EXE、依赖声明、构建脚本、自动测试、Markdown 文档与截图。压缩包排除运行数据库、缓存、虚拟环境和构建临时文件。EXE 校验值位于 `releases/windows/SHA256SUMS.txt`。

## 多语言验证

语言测试覆盖三语资源键和占位符、默认英语、无效语言代码、偏好写入失败、学习数据隔离、CSV 表头以及稳定 JSON 字段。Qt 控件测试通过实际语言按钮，验证未提交答案、判分反馈、最后一题完成结果及重启偏好。

实际浏览器中，`SMALL` 在语言切换后仍保留并判为正确；两题练习的 50% 成绩在再次切换语言后仍保持，刷新保留语言选择。教师导航与表单也按语言显示。文档检查验证三语入口、相对文件与标题链接。

[返回 README](../../README.zh-CN.md)
