# 驗收記錄

[English](../en/verification.md) | [简体中文](../zh-CN/verification.md) | **繁体中文**

驗證日期：2026-09-25。交付版本：3.1.0。

## 自動測試

| 範圍 | 結果 |
| --- | --- |
| JavaScript 業務邏輯與語言切換 | 18 項通過 |
| Python 服務、靜態 HTTP 預覽與 Qt 控件與語言切換 | 23 項通過 |
| 合計 | 41 項通過 |

JavaScript 測試包含免登錄初始化、示例標記、詞庫增改停用、重複驗證、身份界面約束、抽樣、答案歸一化、別名、重複提交、續接、題目快照、錯詞移除、放棄練習、加權統計、JSON / CSV 導出、公式轉義、瀏覽器存儲失敗、重置與訪客數據隔離。

Python 測試包含對應核心規則、SQLite 併發重複提交、損壞數據庫保留、本地靜態資源訪問與目錄邊界。Qt 測試通過真實控件打開教師詞彙編輯器、填寫並儲存詞條，切回學生後搜索、開始練習、提交正確答案和查看記錄。

可復現命令：

```bash
npm test
npm run check:docs
python -m pytest -q
```

Python 測試依賴 `requirements-dev.txt`，Qt 控件測試使用臨時數據庫。網站測試使用隔離的內存存儲。

## 運行驗證

- Windows Qt 源碼：學生概覽與教師詞庫界面已渲染並檢查，截圖位於 `doc/images/`。
- Windows 桌面 EXE：使用臨時數據目錄啓動，直接顯示學生頁面，初始化 178 詞與 3 條示例記錄，截圖儲存成功，退出碼為 0。
- Windows 網站 EXE：實際啓動並返回首頁、JavaScript 引擎和 178 詞 JSON，驗證後關閉測試進程。
- 實際瀏覽器：直接進入學生頁；切到教師新增 `robot / 機器人 / 科技` 和別名 `a robot`；切回學生完成該主題練習，`A ROBOT` 判分正確；刷新後保持 179 詞與本次 100% 的成績記錄。
- 頁面檢查：教師導航只含管理概覽、詞庫管理、練習記錄；學生導航只含學習功能；顯示名稱為 KET Word Studio；瀏覽器控制枱未發現錯誤。
- 網站根路徑與倉庫子路徑均可加載。所有資源均採用相對地址。
- GitHub Pages：部署工作流成功，正式 HTTPS 地址返回 200；實際瀏覽器驗證學生 / 教師切換及各自導航、完成 1 道練習、`SHOUT` 大寫答案判為正確，刷新後保留 100% 的本次成績；控制枱未發現錯誤。
- 在線快捷方式：Windows `.url` 和 Mac `.webloc` 均指向正式 GitHub Pages 地址，文件格式與目標地址已校驗。

## 驗證邊界

已在 Windows 環境進行測試。沒有在真實 macOS 或 Safari 設備上驗收，也沒有聲稱提供 macOS 原生安裝包。網站提供窄屏樣式，但尚未進行完整的移動設備兼容矩陣測試。

網站已發佈至 [GitHub Pages](https://kriswang55.github.io/ket-word-studio/)。後續重新部署時，應按 `deployment.md` 檢查最終 HTTPS 地址。

## 提交內容

源碼目錄、兩種 Windows EXE、依賴聲明、構建腳本、自動測試、Markdown 文檔與截圖。壓縮包排除運行數據庫、緩存、虛擬環境和構建臨時文件。EXE 校驗值位於 `releases/windows/SHA256SUMS.txt`。

## 多語言驗證

語言測試覆蓋三語資源鍵和佔位符、默認英語、無效語言代碼、偏好寫入失敗、學習數據隔離、CSV 表頭以及穩定 JSON 字段。Qt 控件測試通過實際語言按鈕，驗證未提交答案、判分反饋、最後一題完成結果及重啓偏好。

實際瀏覽器中，`SMALL` 在語言切換後仍保留並判為正確；兩題練習的 50% 成績在再次切換語言後仍保持，刷新保留語言選擇。教師導航與表單也按語言顯示。文檔檢查驗證三語入口、相對文件與標題鏈接。

[返回 README](../../README.zh-HK.md)
